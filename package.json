{
  "name": "gmail-alias-generator",
  "version": "1.0.0",
  "private": true,
  "description": "Gmail alias generator for Cloudflare Pages",
  "homepage": "https://main.gmail-alias-generator.pages.dev",
  "license": "MIT",
  "scripts": {
    "build": "echo static site ready",
    "dev": "wrangler pages dev public",
    "deploy": "wrangler pages deploy public",
    "test": "node test-alias-generator.js",
    "check": "node --check public/app.js && node --check public/alias-generator.js && node test-alias-generator.js"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
